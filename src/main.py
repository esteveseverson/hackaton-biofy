

if __name__ == '__main__':
    # settings = Settings()
    # pdf_processor = PDFProcessor(settings.GROQ_API_KEY)

    # PDF_PATH = 'src/data_extraction/files/'

    # all_pdfs = glob(PDF_PATH + '*.pdf')
    # for pdf in all_pdfs:
    #     pdf_processor.process_pdf(pdf)

    # resposta = pdf_processor.query_groq("Me responda se vc consegue ler os arquivos enviados. Sobre oq um deles fala!? Meu querido!")

    print(resposta)
